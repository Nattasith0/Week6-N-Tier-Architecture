# 📊 ANALYSIS.md - Week 6 N-Tier Architecture Analysis

## การวิเคราะห์และเปรียบเทียบ 4 Architectures (Week 3-6)

**ชื่อ-นามสกุล:** ณัฐสิทธิ์ มะโนชัย
**รหัสนักศึกษา:** 67543210056-7
**วันที่ส่ง:** 19/01/2569

---

## คำถาม 1: เปรียบเทียบ 4 Architectures (15 คะแนน)

### ตารางเปรียบเทียบ

| ด้าน                    | Week 3 (Monolithic)                  | Week 4 (Layered)                                       | Week 5 (Client-Server)                                          | Week 6 (N-Tier)                                                        |
| ----------------------- | ------------------------------------ | ------------------------------------------------------ | --------------------------------------------------------------- | ---------------------------------------------------------------------- |
| **Database**            | SQLite                               | SQLite                                                 | SQLite                                                          | PostgreSQL                                                             |
| **Web Server**          | ไม่มี (Node เสิร์ฟเอง/เปิดไฟล์ตรง)   | ไม่มี (Node เสิร์ฟเอง)                                 | ไม่มีเว็บเซิร์ฟเวอร์เฉพาะ (Client แยก แต่ยังพึ่ง Node เป็นหลัก) | Nginx (Reverse Proxy + Static + SSL Termination)                       |
| **Protocol**            | Local/HTTP ภายใน (มักเข้าผ่าน http)  | Local/HTTP ภายใน                                       | HTTP/REST ระหว่าง Client ↔ Server                               | HTTPS (Client→Nginx) + HTTP ภายใน (Nginx→Node) + TCP (Node→PostgreSQL) |
| **Separation Type**     | แยกแบบ “ไม่แยก” ทุกอย่างรวมกัน       | **Layer (Logical)** แยกตามหน้าที่ แต่ยัง Process เดียว | แยก **Process** (Client/Server)                                 | แยก **Tier (Physical)** (Web/App/DB) + ภายใน App ยังเป็น Layered       |
| **Number of Processes** | 1 (โดยรวม)                           | 1 (โดยรวม)                                             | 2 (Browser/Frontend + Backend)                                  | ≥3 (Nginx + Node + PostgreSQL)                                         |
| **Network Required**    | ไม่จำเป็นมาก (รันเครื่องเดียวก็ได้)  | ไม่จำเป็นมาก                                           | จำเป็น (ต้องสื่อสารผ่าน HTTP)                                   | จำเป็น (หลายชั้นสื่อสารกัน + HTTPS)                                    |
| **Scalability**         | ต่ำ (ต้องยกทั้งก้อน)                 | ต่ำ-ปานกลาง (โค้ดดีขึ้นแต่ยังยกทั้งก้อน)               | ปานกลาง (scale server ได้)                                      | สูง (scale ทีละ tier, ทำ load balance ได้ในอนาคต)                      |
| **Security Level**      | ต่ำ (มักไม่มี HTTPS, DB เข้าถึงง่าย) | ต่ำ-ปานกลาง                                            | ปานกลาง (แยก server แล้ว แต่ถ้าไม่มี HTTPS ยังเสี่ยง)           | สูง (HTTPS, DB ไม่ expose, ลดพื้นที่โจมตี)                             |
| **Complexity**          | ต่ำ                                  | ปานกลาง                                                | ปานกลาง                                                         | สูง (หลาย component + config)                                          |
| **Deploy Difficulty**   | ง่ายที่สุด                           | ง่าย-ปานกลาง                                           | ปานกลาง                                                         | ยากที่สุด (ต้องตั้ง Nginx/SSL/DB/PM2)                                  |
| **Development Speed**   | เร็วมาก (เริ่มง่าย)                  | เร็ว (แต่ต้องจัดโครงสร้าง)                             | ปานกลาง                                                         | ช้ากว่า (ต้อง setup infra เพิ่ม)                                       |
| **Production Ready**    | ไม่เหมาะ                             | ยังไม่เหมาะ (ยังไม่มี web tier/security)               | เริ่มใกล้เคียง (แต่ยังต้องเพิ่ม web server/https/db)            | เหมาะสุด (เข้าใกล้ production pattern)                                 |

### อธิบายเพิ่มเติม:

**Week 3 (Monolithic):**

```
- ข้อดีหลัก: ทำเร็ว โค้ดอยู่ที่เดียว รันง่าย เหมาะกับการเรียนและทดลองฟีเจอร์
- ข้อเสียหลัก: ทุกอย่างผูกกันแน่น แก้จุดเดียวอาจกระทบทั้งระบบ และ scale ยาก
- เหมาะกับ: งานเล็กๆ, prototype, การบ้าน/การทดลองที่ผู้ใช้ไม่มาก
```

**Week 4 (Layered):**

```
- ข้อดีหลัก: โค้ดเป็นระเบียบ แยก responsibility ชัด ทำให้แก้ง่ายและทดสอบง่ายขึ้น
- ข้อเสียหลัก: ยังเป็น process เดียว ดังนั้นเรื่อง performance/scale ยังติดคอขวดเดิม
- เหมาะกับ: โปรเจกต์ขนาดเล็ก-กลางที่อยากให้โค้ดดูแลต่อได้ง่าย
```

**Week 5 (Client-Server):**

```
- ข้อดีหลัก: แยก frontend/backend เป็นคนละ process ทำงานร่วมกันผ่าน REST API ได้จริงเหมือนระบบเว็บทั่วไป
- ข้อเสียหลัก: ถ้าไม่มี HTTPS ยังเสี่ยง และ backend ต้องแบกรับทั้งงาน API/Static มากขึ้น
- เหมาะกับ: เว็บแอปทั่วไป, ทีมเริ่มแบ่งงาน frontend กับ backend
```

**Week 6 (N-Tier):**

```
- ข้อดีหลัก: มี Web Server (Nginx) ทำ SSL + เสิร์ฟ Static เร็วขึ้น และใช้ PostgreSQL ที่รองรับ concurrent ได้ดีกว่า SQLite
- ข้อเสียหลัก: ซับซ้อนขึ้น ต้องดูแลหลาย service และ config (nginx, ssl, pm2, db)
- เหมาะกับ: ระบบที่ต้องใช้จริง/ใกล้ production, ต้องการ security และรองรับผู้ใช้มากขึ้น
```

---

## คำถาม 2: Quality Attributes Radar Chart (10 คะแนน)

### ตารางคะแนน (1-5 คะแนน)

| Quality Attribute   | Week 3 | Week 4 | Week 5 | Week 6 | หมายเหตุ                                       |
| ------------------- | ------ | ------ | ------ | ------ | ---------------------------------------------- |
| **Performance**     | 2      | 3      | 3      | 4      | W6 ใช้ Nginx เสิร์ฟ static + proxy ลดภาระ Node |
| **Scalability**     | 1      | 2      | 3      | 5      | W6 scale แยก tier ได้                          |
| **Security**        | 1      | 2      | 3      | 5      | W6 มี HTTPS + DB ไม่ expose                    |
| **Maintainability** | 2      | 4      | 4      | 5      | W6 มีทั้ง Layered ใน App + แยก Tier            |
| **Testability**     | 2      | 4      | 4      | 5      | แยกชั้น/แยก service ทำให้เทสง่ายขึ้น           |
| **Deployability**   | 5      | 4      | 3      | 2      | W6 deploy ยากสุดเพราะหลาย component            |
| **Availability**    | 2      | 2      | 3      | 4      | W6 แยก tier ทำให้จัดการ uptime ได้ดีขึ้น       |
| **Modifiability**   | 2      | 4      | 4      | 5      | แก้ได้เป็นชั้นๆ และแยก infra                   |
| **รวม**             | 17/40  | 25/40  | 27/40  | 35/40  | รวมตามเหตุผลด้านบน                             |

### คำอธิบายการให้คะแนน:

**Performance (ความเร็ว):**

```
- Week 3: 2 คะแนน เพราะทุกอย่างอยู่รวมกัน Node ต้องทำหลายหน้าที่ และ SQLite มีข้อจำกัดเรื่องเขียนพร้อมกัน
- Week 4: 3 คะแนน เพราะโค้ดเป็นชั้น ทำงานเป็นระบบขึ้น แต่ยัง process เดียวและยังใช้ SQLite
- Week 5: 3 คะแนน เพราะแยก client/server ลดภาระบางส่วน แต่ backend ยังอาจเสิร์ฟ static และยังใช้ SQLite
- Week 6: 4 คะแนน เพราะ Nginx เสิร์ฟ static ได้เร็วและ proxy ไป backend เฉพาะ API + ใช้ PostgreSQL
```

**Scalability (รองรับการขยาย):**

```
- Week 3: 1 คะแนน เพราะต้อง scale ทั้งก้อนและติดข้อจำกัด SQLite
- Week 4: 2 คะแนน เพราะแม้โค้ดแยก layer แต่ยังต้อง scale ทั้งแอปพร้อมกัน
- Week 5: 3 คะแนน เพราะสามารถ scale backend ได้ (เช่นรันหลาย instance) แต่ยังขาด web tier และ DB ที่เหมาะ
- Week 6: 5 คะแนน เพราะ scale ได้เป็น tier (เพิ่ม app server, ปรับ nginx/load balance, แยก DB)
```

**Security (ความปลอดภัย):**

```
- Week 3: 1 คะแนน เพราะมักไม่มี HTTPS และการแยกขอบเขตระบบไม่ชัด
- Week 4: 2 คะแนน เพราะโค้ดเป็นระบบขึ้น แต่ยังไม่มี web tier/https
- Week 5: 3 คะแนน เพราะมี server ชัดเจน แต่ถ้า HTTP ยังเสี่ยง sniffing
- Week 6: 5 คะแนน เพราะมี HTTPS (SSL termination) และ DB อยู่หลัง backend ไม่เปิดสาธารณะ
```

**Maintainability (ความสะดวกในการดูแล):**

```
- Week 3: 2 คะแนน เพราะโค้ดรวมกัน แก้แล้วกระทบหลายส่วน
- Week 4: 4 คะแนน เพราะแยก controller/service/repository ทำให้แก้เฉพาะชั้นได้
- Week 5: 4 คะแนน เพราะแยก frontend/backend ทำให้ดูแลแยกกันได้
- Week 6: 5 คะแนน เพราะทั้งแยก layer ใน app และแยก tier ของ infra ชัดเจนที่สุด
```

**Testability (ความสะดวกในการทดสอบ):**

```
- Week 3: 2 คะแนน เพราะเทสยาก ต้องรันทั้งระบบรวมกัน
- Week 4: 4 คะแนน เพราะสามารถเทส service/repository แยกได้มากขึ้น
- Week 5: 4 คะแนน เพราะเทส API ผ่าน HTTP ได้เป็นชุด (integration test)
- Week 6: 5 คะแนน เพราะมี endpoint health + ทดสอบผ่าน https/nginx ได้ และแยก component ชัด
```

**Deployability (ความสะดวกในการ Deploy):**

```
- Week 3: 5 คะแนน เพราะแค่รัน node ตัวเดียวก็ใช้ได้
- Week 4: 4 คะแนน เพราะยังเป็นแอปเดียว แต่ต้องจัดโครงสร้างไฟล์มากขึ้น
- Week 5: 3 คะแนน เพราะต้อง deploy client/server และตั้งค่า CORS/paths
- Week 6: 2 คะแนน เพราะต้องติดตั้ง PostgreSQL, Nginx, SSL, PM2 และ config หลายจุด
```

---

## คำถาม 3: สถานการณ์การใช้งาน (10 คะแนน)

### สถานการณ์ A: Startup MVP

**Architecture ที่เลือก:** [x] Week 3 / [ ] Week 4 / [ ] Week 5 / [ ] Week 6

**เหตุผล:**

```
เลือก Week 3 เพราะเป้าหมายคือทำ MVP ให้เร็วที่สุด ทีมมี 2 คนและเป็น junior ต้องลดความซับซ้อนของระบบ
Monolithic ช่วยให้พัฒนาและแก้ไขไว รับ feedback จากลูกค้าได้เร็ว และค่าใช้จ่าย infra ต่ำ
เมื่อเริ่มมีผู้ใช้เพิ่มและโค้ดเริ่มใหญ่ ค่อย evolve ไป Layered/Client-Server ในรอบถัดไป
```

---

### สถานการณ์ B: E-commerce Platform

**Architecture ที่เลือก:** [ ] Week 3 / [ ] Week 4 / [ ] Week 5 / [x] Week 6

**เหตุผล:**

```
เลือก Week 6 เพราะผู้ใช้ 100,000 คน และมี flash sale ต้องรองรับโหลดสูง + ความปลอดภัยเรื่อง payment
Nginx ช่วยรับโหลด/เสิร์ฟ static/ทำ SSL ได้ดี และ PostgreSQL รองรับ transaction และ concurrent ได้เหมาะกับงานขาย
การแยก tier ทำให้ scale app server เพิ่มได้ และจัด security boundary ให้ DB ไม่ถูกเข้าถึงโดยตรง
```

---

### สถานการณ์ C: Internal Company Tool

**Architecture ที่เลือก:** [ ] Week 3 / [x] Week 4 / [ ] Week 5 / [ ] Week 6

**เหตุผล:**

```
เลือก Week 4 เพราะผู้ใช้มีเพียง 50 คน ใช้ในองค์กรผ่าน VPN ไม่ได้ต้อง scale ใหญ่มาก
Layered ทำให้โค้ดดูแลต่อได้ง่ายในระยะยาวและเทสง่าย แต่ยังไม่เพิ่มความซับซ้อนของ infra แบบ N-Tier
หากอนาคตต้องเปิดให้ใช้นอกองค์กรหรือมีผู้ใช้เพิ่ม ค่อย evolve ไป Client-Server หรือ N-Tier
```

---

### สถานการณ์ D: Banking Application

**Architecture ที่เลือก:** [ ] Week 3 / [ ] Week 4 / [ ] Week 5 / [x] Week 6

**เหตุผล:**

```
เลือก Week 6 เพราะระบบธนาคารต้อง security สูง มี compliance/audit และผู้ใช้จำนวนมาก
N-Tier ช่วยแยกขอบเขตชัด: web tier ทำ TLS, app tier บังคับ business rules, db tier ถูกซ่อนไว้หลังระบบ
นอกจากนี้ PostgreSQL รองรับ transaction (ACID) เหมาะกับข้อมูลทางการเงิน และสามารถขยายระบบในอนาคตได้
```

---

## คำถาม 4: ประสบการณ์จากการทำ Lab (5 คะแนน)

### ก. ปัญหาที่พบในการทำ Week 6:

| # | ปัญหา                                                         | สาเหตุ                                                             | วิธีแก้ไข                                                                 |
| - | ------------------------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| 1 | `apt install` ติดปัญหา connect `th.archive.ubuntu.com` ไม่ได้ | mirror ในไทยเชื่อมต่อไม่ได้/ถูกปฏิเสธ                              | เปลี่ยน repository ไปใช้ `archive.ubuntu.com` แล้ว `sudo apt update` ใหม่ |
| 2 | `psql` ไม่เจอ (`Command 'psql' not found`)                    | ยังไม่ได้ติดตั้ง postgresql-client จาก repo ที่โหลดไม่ครบ          | ติดตั้ง package ที่จำเป็นจน `psql --version` ใช้งานได้                    |
| 3 | `Cannot GET /api/health` ตอนทดสอบ                             | พอร์ต 3000 ถูกโปรเจกต์เก่าจับอยู่ (midterm-bonus) ทำให้เรียกผิดแอป | ตรวจด้วย `ss/ps`, kill process ที่จับ 3000 แล้ว start week6 ด้วย PM2      |

### ข. เวลาที่ใช้ในแต่ละส่วน:

| ส่วนงาน               | เวลาที่คาด | เวลาจริง  | หมายเหตุ                                       |
| --------------------- | ---------- | --------- | ---------------------------------------------- |
| ติดตั้ง PostgreSQL    | 20 นาที    | 30 นาที   | ติดปัญหา apt mirror                            |
| ติดตั้ง Nginx         | 10 นาที    | 10 นาที   | ติดตั้งปกติ                                    |
| สร้าง SSL Certificate | 10 นาที    | 10 นาที   | self-signed ทำได้ตามคำสั่ง                     |
| Migrate Database      | 15 นาที    | 15 นาที   | ใช้ init.sql สร้าง schema + seed               |
| ตั้งค่า Nginx Config  | 20 นาที    | 25 นาที   | ตรวจและ reload หลายรอบ                         |
| Testing               | 15 นาที    | 20 นาที   | แก้ปัญหา port + แก้ test-api.sh เรื่อง curl -k |
| **รวม**               | ~90 นาที   | ~110 นาที | รวมเวลาแก้ปัญหา                                |

### ค. สิ่งที่ได้เรียนรู้ใหม่:

```
1. การแยก Tier vs Layer และผลกระทบต่อการ deploy และ scale
2. การติดตั้งและใช้งาน PostgreSQL รวมถึง schema/trigger
3. การตั้งค่า Nginx เป็น reverse proxy และเสิร์ฟ static files
4. การสร้าง self-signed certificate และแนวคิด SSL termination
5. วิธี debug ปัญหา port/Process ด้วย ss, ps, /proc และการจัดการ PM2
```

---

## คำถาม 5: Evolution Path (5 คะแนน)

### เมื่อไหร่ควร Evolve จาก Architecture หนึ่งไปอีกแบบ?

**จาก Monolithic → Layered:**

```
Trigger/เงื่อนไข:
1. โค้ดเริ่มใหญ่ อ่านยาก และแก้ไขแล้วกระทบหลายส่วน
2. ต้องการแยกความรับผิดชอบ (controller/service/repo) ให้ชัด
3. ต้องการเพิ่ม testability และลด technical debt
```

**จาก Layered → Client-Server:**

```
Trigger/เงื่อนไข:
1. ต้องการให้ frontend/backend แยกทีมพัฒนาและ release แยกกัน
2. ต้องการให้ client เรียกใช้ API ผ่านเครือข่ายจริง (REST)
3. ต้องรองรับผู้ใช้หลายคน และต้องจัดการ CORS/Authentication ในอนาคต
```

**จาก Client-Server → N-Tier:**

```
Trigger/เงื่อนไข:
1. ต้องการ HTTPS และ web server ทำ reverse proxy/เสิร์ฟ static ให้มีประสิทธิภาพ
2. SQLite เริ่มไม่พอ (concurrent write, reliability) ต้องย้ายไป DB ที่เหมาะ เช่น PostgreSQL
3. ต้องการ scale และ security boundary ที่ชัดเจน (ไม่ expose DB)
```

**จาก N-Tier → Microservices:**

```
Trigger/เงื่อนไข:
1. ระบบใหญ่จนทีมหลายทีมทำงานชนกัน ต้องแยก service ตาม domain
2. ต้อง deploy/scale รายส่วนอย่างอิสระ และมี SLA แยกกัน
3. มีความต้องการด้าน resilience สูง (fault isolation) และต้องการ independent lifecycle
```

### Decision Flowchart:

```
                              เริ่มโปรเจกต์ใหม่
                                    │
                                    ▼
                    ┌───────────────────────────────┐
                    │  ทีมมีประสบการณ์น้อย?         │
                    │  งบประมาณจำกัด?                │
                    │  Timeline สั้น?                 │
                    └───────────────┬───────────────┘
                                    │
                        ┌───────────┴───────────┐
                        │ Yes                   │ No
                        ▼                       ▼
                   [Monolithic]            [Layered]
                        │                       │
            ┌───────────┴───────────┐           │
            │ โค้ดเริ่มใหญ่ขึ้น?     │           │
            │ ต้องการ Testability?    │           │
            └───────────┬───────────┘           │
                        │                        │
            ┌───────────┴───────────┐            ▼
            │ Yes                   │      ต้องแยก Client/Server?
            ▼                       │            │
       [Layered]                    │   ┌────────┴────────┐
            │                       │   │ Yes             │ No
            ▼                       │   ▼                 ▼
   ต้องมี Frontend แยกทีม?          │ [Client-Server]   [Layered]
            │                       │        │
   ┌────────┴────────┐              │        ▼
   │ Yes             │ No           │   ต้องมี HTTPS/DB รองรับโหลดสูง?
   ▼                 ▼              │        │
[Client-Server]   [Layered]         │   ┌────┴────┐
        │                            │   │ Yes     │ No
        ▼                            │   ▼         ▼
  ต้องมี HTTPS/Scale/DB จริง?        │ [N-Tier] [Client-Server]
        │
   ┌────┴────┐
   │ Yes     │ No
   ▼         ▼
[N-Tier] [Client-Server]
```

---

## คำถาม 6: บทเรียนสำคัญ (5 คะแนน)

### Top 3 บทเรียนจากการทำ Lab Week 3-6:

**บทเรียนที่ 1:**

```
หัวข้อ: Tier vs Layer ไม่เหมือนกัน
รายละเอียด: Layer คือการแยกโค้ดตามหน้าที่ (logical) แต่ Tier คือการแยกการรันจริง (physical) เช่น Nginx/Node/DB
จะนำไปใช้อย่างไร: เวลาออกแบบระบบจะเลือกแยกให้เหมาะกับปัญหา เช่น อยากดูแลง่ายเริ่มจาก Layered ก่อน แต่ถ้าต้อง production ต้องมี Tier ชัด
```

**บทเรียนที่ 2:**

```
หัวข้อ: Infrastructure มีผลต่อคุณภาพระบบ
รายละเอียด: แค่ย้ายจาก SQLite → PostgreSQL และเพิ่ม Nginx + HTTPS ทำให้ระบบรองรับ concurrent และปลอดภัยขึ้นมาก
จะนำไปใช้อย่างไร: เวลาออกโปรเจกต์จริงต้องคิด NFR (security/performance/reliability) ตั้งแต่ต้น ไม่ใช่คิดทีหลัง
```

**บทเรียนที่ 3:**

```
หัวข้อ: การ Debug ต้องมองทั้งระบบ
รายละเอียด: ปัญหา "Cannot GET" ไม่ได้มาจากโค้ดเสมอ แต่เกิดจาก port ถูกโปรเจกต์อื่นจับอยู่ ต้องใช้ ss/ps/pm2 ช่วยตรวจ
จะนำไปใช้อย่างไร: เวลาเจอปัญหา จะไล่จาก network → process → config → code ตามลำดับ เพื่อแก้ได้เร็วและตรงจุด
```

### ถ้าเริ่มทำใหม่ตั้งแต่ Week 3 จะทำอะไรต่างไป?

```
1. ตั้งโครงสร้าง Layered ตั้งแต่แรก เพื่อลดการ refactor ในภายหลัง
2. เขียนสคริปต์ทดสอบ API และ health check ตั้งแต่ Week 5 เพื่อจับปัญหาได้เร็ว
3. วางแผน port/service ให้ชัด (ใช้ PM2 name และ delete ของเก่า) ลดปัญหา port conflict
```

### ทักษะที่ได้พัฒนามากที่สุด:

| ทักษะ                 | ระดับก่อนทำ (1-5) | ระดับหลังทำ (1-5) | หมายเหตุ                                         |
| --------------------- | ----------------- | ----------------- | ------------------------------------------------ |
| Linux/Ubuntu          | 2                 | 4                 | ใช้ apt, systemctl, permissions, troubleshooting |
| Database (SQL)        | 2                 | 4                 | สร้าง schema, trigger, query, connection         |
| Web Server (Nginx)    | 1                 | 4                 | reverse proxy, static, SSL termination           |
| Node.js/Express       | 3                 | 4                 | route/middleware/health endpoint                 |
| REST API              | 3                 | 4                 | CRUD + test script                               |
| Git/Version Control   | 3                 | 4                 | จัด repo/commit/re-run                           |
| Networking            | 2                 | 4                 | ports, hosts file, localhost vs domain           |
| Security (SSL/HTTPS)  | 1                 | 4                 | self-signed, curl -k, https flow                 |
| Architecture Thinking | 2                 | 4                 | เข้าใจ trade-off และการ evolve architecture      |

### สิ่งที่ยังสับสนหรืออยากเรียนรู้เพิ่ม:

```
1. การทำ SSL certificate แบบ trusted (Let's Encrypt) และ renew อัตโนมัติ
2. การทำ load balancing หลาย app server ด้วย Nginx
3. แนวทาง migrate ไป microservices และการจัดการ observability (logs/metrics/tracing)
```

---

## 📸 Screenshots

### Screenshot 1: Services Status

```
(แนบรูป: sudo systemctl status postgresql nginx + pm2 status)
```

### Screenshot 2: HTTPS in Browser

```
(แนบรูป: Browser แสดง https://taskboard.local พร้อมคำเตือน self-signed/หรือ lock icon หลัง proceed)
```

### Screenshot 3: API Response

```
(แนบรูป: curl -k https://taskboard.local/api/tasks)
```

### Screenshot 4: Task Board UI Working

```
(แนบรูป: หน้า Task Board พร้อม tasks)
```

### Screenshot 5: Create/Edit Task

```
(แนบรูป: สร้างหรือแก้ไข task)
```

---

## 🔗 Links

* **GitHub Repository:** [Github](https://github.com/Nattasith0/Week6-N-Tier-Architecture)
* **VM IP Address:** 192.168.56.101
* **API Endpoint:** [https://taskboard.local/api](https://taskboard.local/api)

---

## ✅ Self-Check Before Submit

* [ ] ตารางเปรียบเทียบกรอกครบทุกช่อง
* [ ] Quality Attributes ให้คะแนนและอธิบายครบ
* [ ] สถานการณ์ 4 ข้อ ตอบครบทุกข้อ
* [ ] ปัญหาและวิธีแก้ไขระบุชัดเจน
* [ ] Evolution Path วาดครบ
* [ ] บทเรียนสำคัญ 3 ข้อ
* [ ] Screenshots ครบ 5 รูป
* [ ] Push ไป GitHub แล้ว

---

**หมายเหตุ:** เอกสารนี้มีน้ำหนัก **37.5%** ของคะแนนทั้งหมด โปรดตอบอย่างละเอียดและจริงใจ!

---

*ENGSE207 - Software Architecture - Week 6*
*มหาวิทยาลัยเทคโนโลยีราชมงคลล้านนา*
